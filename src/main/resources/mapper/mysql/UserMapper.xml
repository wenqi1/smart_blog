<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.learn.blog.dao.UserDao" >

    <insert id="insertUserInfo" parameterType="com.learn.blog.model.User" useGeneratedKeys="true">
        insert into user(user_name,user_password,user_telephone_number)
        values(#{userName}, #{userPassword}, #{userTelephoneNumber})
    </insert>

    <update id="updateUserInfo" parameterType="com.learn.blog.model.User">
        update user
        <set>
            <if test="userPassword != null">
                user_password = #{userPassword, jdbcType=VARCHAR},
            </if>
            <if test="userEmail != null">
                user_email = #{userEmail, jdbcType=VARCHAR},
            </if>
        </set>
        WHERE user_id = #{userId, jdbcType=INTEGER}
    </update>

    <select id="getUserInfoById" resultType="com.learn.blog.model.User">
        select * from user where user_id = #{userId}
    </select>

    <select id="getUserInfoByUsername" resultType="com.learn.blog.model.User">
        select * from user where user_name = #{userName} and user_password = #{userPassword}
    </select>

</mapper>